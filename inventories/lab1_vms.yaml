# GENERATED FROM TEMPLATE
all:
  children:
    nodes:
      hosts:
        small1-lab1:
          ansible_user: ubuntu
          ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q lab1"'
          cp: false
        medium1-lab1:
          ansible_user: ubuntu
          ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q lab1"'
          cp: false
        medium2-lab1:
          ansible_user: ubuntu
          ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q lab1"'
          cp: false
        cp-lab1:
          ansible_user: ubuntu
          ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p -q lab1"'
          cp: true
